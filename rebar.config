{erl_opts, [debug_info]}.

{pre_hooks, [
  {clean, "rm -rf ./_build/test"}
]}.
{provider_hooks, [{pre, [{clean}]}]}.

{cover_enabled, true}.
{eunit_opts, [
  verbose, {
    report, {
      eunit_surefire, [
        {dir, "./_build/test"}
      ]
    }
  }
]}.
